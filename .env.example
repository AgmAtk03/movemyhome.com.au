# Public Vite env — baked into the client bundle at build time. Never commit a real key.
# Enable Maps JavaScript API + Places API + Directions API, then restrict this key to HTTP referrers.
VITE_GOOGLE_MAPS_API_KEY=
