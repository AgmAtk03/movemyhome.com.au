# Aama Removals — static preview

Single-file public export of the quote wizard. No GEMINI_API_KEY or Maps key is baked in.

Set `VITE_GOOGLE_MAPS_API_KEY` on Netlify/Vercel and rebuild to enable Places Autocomplete + Directions.
