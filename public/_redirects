# Vite copies this file into dist/. Netlify checks _redirects before
# netlify.toml, so /api/* must be first or POST /api/* becomes a 404 HTML page.
/api/*  https://aama-removals.vercel.app/api/:splat  200!
/*      /index.html   200
